using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExploadingBalloon : MonoBehaviour
{
    public int ExploadingBalloonCount = 0;
    
    // Start is called before the first frame update
    void Start()
    {
        ExploadingBalloonCount = 1;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
